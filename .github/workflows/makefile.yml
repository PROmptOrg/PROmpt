name: Makefile CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    
    - name: Run check
      run: make kong-postgres-kafka

    - name: Check api gateway
      run: curl -i -X GET http://example.com
